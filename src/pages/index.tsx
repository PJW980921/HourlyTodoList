import { PoorStory } from '@/styles/fonts/fonts';

export default function Home() {
  return (
    <main className={`${PoorStory.className}`}>
      <span className=" text-[2rem]">HourlyTodoList</span>
    </main>
  );
}
